This folder intentionally only documents the move of the Ultralytics smoke script.

Canonical file: **`../pipeline/stages/stage_04_inference_tracking/load_and_infer.py`** (referenced from **`Dockerfile.cpu`**, **`docker-compose`** `cpu-smoke`, and the docs).
