# Yolo pose miniature split

Purpose:

- Validate `data.yaml` path conventions rapidly.
- Run smoke-training iterations before relocating terabyte corpora.

Hashes in filenames originate from upstream Roboflow exports. Extend privately with supplementary captured frames respecting identical label schema (`kpt_shape [3,3]`).
