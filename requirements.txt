ultralytics>=8.3.0
opencv-python>=4.8.0
pandas>=2.0.0
numpy>=1.24.0
torch>=2.0.0
torchvision>=0.15.0
PyYAML>=6.0

# Reproducible Docker builds pin versions in requirements-docker.txt (see Dockerfile.gpu).
